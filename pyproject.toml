[project]
name = "community"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
]

[project.optional-dependencies]
dev = [
    "ipykernel>=7.2.0",
]
